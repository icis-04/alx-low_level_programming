#include "main.h"
#include <stdio.h>
/**
 * main - gets the number of arguments passed to it
 * @argc: input
 * @argv: input
 * Return: integer
 */
int main(int argc, char* argv[] __attribute__((unused)))
{
printf("%d\n", argc - 1);
return (0);
}
