#include "main.h"
/**
 * _islower - checks if number is lowercase.
 * Return: 0 or 1.
 * c - return int.
 */
int _islower(int c)
{
if ((c >= 'a') && (c <= 'z'))
{
return (1);
} 
else { 
return (0);
}
_putchar('\n');
}

