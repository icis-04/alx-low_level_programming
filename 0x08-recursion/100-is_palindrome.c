#include "main.h"
/**
 * is_palindrome - checks if a string is a palindrome
 * @s: input string 
 * Return: returns 1 for success or 0 for failure
 */
int is_palindrome(char *s)
{
char i[];
char *s[] = i;
if (_puts_recursion(i) == _print_rev_recursion(i))
{
return (1);
}
else
{
return (0);
}
}
